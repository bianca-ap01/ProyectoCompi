int main() {
    auto counter = 0;
    auto limit   = 5;

    while (counter < limit) {
        printf("%d\n", counter);
        counter = counter + 1;
    }

    if (limit > 3) {
        auto msg = 1;
        printf("%d\n", msg);
    }

    return 0;
}
